[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60    # model load takes ~20s on cold start
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
